name: simple_example
description: Simple example demonstrating chirp logging works out of the box

environment:
  sdk: ^3.0.0

dependencies:
  chirp:
    path: ../../packages/chirp
  clock: ^1.1.0
  shelf: ^1.4.0
  shelf_router: ^1.1.0

dev_dependencies:
  lint: ^2.8.0

sidekick:
  puro:
    useDartSdk: '3.10.1'